def iterate_range(current, stop, step):
    print(current)
    if current == stop:
        return
    elif current < stop:
        iterate_range(current + step, stop, step)
    else:
        iterate_range(current - step, stop, step)

start = 0
stop = 0.5
step = 0.01

iterate_range(start, stop, step)
